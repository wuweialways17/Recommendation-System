1. This project is done by jupter notebook, and i use an additional package "surprise", which is as the same as the scikit-learn

2. You can just run the cells one by one for testing

3. "Data Processing" is not required to run, due to the large amount of initial dataset, and i I have submitted the processed "last_1_years_restaurant_reviews.csv" file together

4. To use "Interface", you just need to input the uid(user id) and iid(item id), then it will return the predicted result（rating star)